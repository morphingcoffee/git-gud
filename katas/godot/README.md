# Godot katas

Godot 4.x kata projects live here as `NN_<name>/` (e.g. `01_evening_news/`).

Each folder graduates from a matching spec in [`incubator/`](../../incubator/). See [kata-gym-level](../../.cursor/skills/kata-gym-level/SKILL.md) for gym vs level workflow.
